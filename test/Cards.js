const { accounts, contract } = require('@openzeppelin/test-environment');
const { expect } = require('chai');

const Cards = contract.fromArtifact("Cards");

describe('Cards', function () {
    beforeEach(async function () {
    });
});
